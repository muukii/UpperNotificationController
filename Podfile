# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'UpperNotificationController' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Reveal-iOS-SDK'

  # Pods for UpperNotificationController

  target 'UpperNotificationControllerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'UpperNotificationControllerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
